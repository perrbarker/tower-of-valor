using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Camera is to fixate on the player that is furthest up the "Tower"
public class Camera : MonoBehaviour {

	// Use this for initialization
	void Awake () 
	{
		
	}
	
	// Update is called once per frame, so make sure to call other functions instead of using lines of code
	void Update () 
	{
		
	}
}
